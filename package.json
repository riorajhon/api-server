{
  "name": "my-backend",
  "version": "1.0.0",
  "description": "Backend API with Firebase",
  "main": "api/firebase.js",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0"
  },
  "engines": {
    "node": ">=18.x"
  }
}
